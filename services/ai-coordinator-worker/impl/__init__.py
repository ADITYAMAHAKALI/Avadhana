"""Concrete implementations of the ports declared under `interfaces/`.

`worker.py` (the composition root) is the only module that should import
from here directly — everything else should depend on the `interfaces/`
Protocols instead.
"""
