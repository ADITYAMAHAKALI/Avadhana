"""Abstract ports (Protocols) for the AI Coordinator Worker.

Each module defines one interface describing a dependency the composition
root (`worker.py`) can inject a concrete implementation for. See `impl/` for
the concrete implementations that satisfy these Protocols.
"""
