"""Marketplace router subpackage (GitHub issues #63, #64, #65 under epic
#62). Mirrors `app/models/marketplace/`'s deliberate deviation from the
flat `app/routers/*.py` layout — see that package's docstring for the
rationale (independent product surface, structured for eventual
extraction into its own service).
"""
